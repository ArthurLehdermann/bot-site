---
// Widget de chat incorporável — dogfood. O bot-site usa exatamente o mesmo
// bundle "1 tag" que entregamos aos clientes, servido pela API do bot.
//
// public_key não é segredo (publishable, como em Stripe) — pode ficar visível.
// bot.bigworks.com.br é o site (este projeto); a API/bundle vivem em
// painel.bot.bigworks.com.br. O domínio precisa estar em allowed_origins do
// tenant (BotDogfoodSeed cuida disso).
const WIDGET_API_URL = import.meta.env.DEV ? 'http://localhost' : 'https://painel.bot.bigworks.com.br';
const WIDGET_PUBLIC_KEY = 'wpk_45Pg4E7BYLG6x2GQqMtpbUaZcMGdZk2c';
---

<script src={`${WIDGET_API_URL}/widget.js`} data-key={WIDGET_PUBLIC_KEY} async is:inline></script>
